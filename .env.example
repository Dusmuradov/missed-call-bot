# Токен бота от @BotFather
BOT_TOKEN=123456:ABCDefGhIJKlmNoPQRsTUVwxyZ

# ID группы операторов (отрицательный для супергрупп/каналов)
TELEGRAM_CHAT_ID=-1001234567890

# ID темы внутри супергруппы (оставьте пустым если тем нет)
TELEGRAM_THREAD_ID=

# Whitelist user_id для доступа к отчётам (через запятую, пустой = все)
# Получить свой ID: написать @userinfobot
REPORT_ALLOWED_USERS=

# Секрет, который Utel шлёт в заголовке каждого webhook-запроса
WEBHOOK_SECRET=замените_на_длинную_случайную_строку

# Имя HTTP-заголовка с секретом (уточните у поддержки Utel)
WEBHOOK_SECRET_HEADER=X-Webhook-Secret

# Таймзона для отображения времени звонков
TIMEZONE=Asia/Tashkent

# Уровень логирования: DEBUG | INFO | WARNING | ERROR
# Включите DEBUG чтобы увидеть Raw webhook payload и найти поле оператора
LOG_LEVEL=INFO

# ---------------------------------------------------------
# База данных (SQLite по умолчанию, Railway Postgres позже)
# ---------------------------------------------------------
# DATABASE_URL=sqlite+aiosqlite:///./data/calls.db
# DATABASE_URL=postgresql+asyncpg://user:pass@host/dbname
DATABASE_URL=

# ---------------------------------------------------------
# Планировщик перезвонов
# ---------------------------------------------------------
# Через сколько минут считать пропущенный звонок "не перезвоненным"
CALLBACK_CHECK_MINUTES=10

# ---------------------------------------------------------
# Операторы (JSON-маппинг extension → имя)
# ---------------------------------------------------------
# OPERATORS_MAP={"101": "Азиз", "102": "Камола", "103": "Санжар"}
OPERATORS_MAP=

# ---------------------------------------------------------
# AmoCRM (один аккаунт)
# ---------------------------------------------------------
# Поддомен: yourcompany.amocrm.ru → AMOCRM_SUBDOMAIN=yourcompany
AMOCRM_SUBDOMAIN=
AMOCRM_CLIENT_ID=
AMOCRM_CLIENT_SECRET=
AMOCRM_REDIRECT_URI=https://your-app.railway.app/amocrm/oauth/callback
# ID первого (входящего) статуса воронки. Оставьте 0 чтобы определять автоматически.
AMOCRM_INITIAL_STATUS_ID=0

# ---------------------------------------------------------
# CRM (username/password → access_token → M2M token)
# ---------------------------------------------------------
# Базовый URL CRM (без trailing slash)
CRM_BASE_URL=
# Учётные данные для входа
CRM_USERNAME=
CRM_PASSWORD=
# Путь для POST логина (можно изменить если у вашего CRM другой)
CRM_LOGIN_PATH=/api/auth/login
# Путь для получения M2M токена по access_token
CRM_M2M_PATH=/api/auth/m2m

# ---------------------------------------------------------
# Hermes AI (DeepSeek)
# ---------------------------------------------------------
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
# Час отправки утреннего AI-дайджеста по менеджерам (локальная timezone)
HERMES_DIGEST_HOUR=9
# Час отправки ежедневного ROP-плана P1/P2/P3 продавцам (по TIMEZONE)
ROP_PLAN_HOUR=9

# ---------------------------------------------------------
# BILLZ POS (ежедневный аналитический дайджест)
# ---------------------------------------------------------
# Секрет для авторизации (secret_token из BILLZ настроек)
BILLZ_SECRET=
# UUID компании (найти через BILLZ API или Admin Panel)
BILLZ_COMPANY_ID=
# UUID платформы (не менять — стандартное значение BILLZ)
BILLZ_PLATFORM_ID=7d4a4c38-dd84-4902-b744-0488b80a4c01
# UUID магазинов через запятую (пусто = все магазины компании)
# Пример: BILLZ_SHOP_IDS=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx,yyyyyyyy-...
BILLZ_SHOP_IDS=
# Валюта для отчётов
BILLZ_CURRENCY=UZS
# Час отправки ежедневного BILLZ-дайджеста (по TIMEZONE)
BILLZ_DIGEST_HOUR=9
